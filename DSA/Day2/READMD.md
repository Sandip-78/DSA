Today i learn about Pair sum and majority element in array.

Pair sum : 
- first learn the brute force approach then in which time complexity becomes 0(n^2)
- then used two poniter approach in which time complexity becomes 0(n)

Majority Element : 
- learn about bruteforce approach in which time complexity becomes O(n^2)
- in optimized code i used sorting so time complexcity becomes O(nlogn) 
- then learn about most optimize approach Moore's voting algorithm in which if element same then frequency-- else  frequency++ so the time complexcity becomes O(nlogn) 